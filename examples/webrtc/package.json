{
  "name": "zod-rpc-webrtc-example",
  "version": "1.0.0",
  "description": "WebRTC example for Zod RPC with peer-to-peer communication",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "serve": "vite preview",
    "start": "npm run build && npm run serve",
    "lint": "eslint '**/*.js'",
    "lint:fix": "eslint '**/*.js' --fix"
  },
  "dependencies": {
    "@xtr-dev/zod-rpc": "file:../..",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.32.0",
    "@vitejs/plugin-basic-ssl": "^2.1.0",
    "eslint": "^9.32.0",
    "serve": "^14.2.4",
    "vite": "^7.0.6"
  }
}
